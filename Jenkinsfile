pipeline {
    agent any

    stages {
        stage("build") {
            steps {
                echo 'building the application ...'
            }
        }

        stage("test") {
            steps {
                echo 'testing the application ...'
                 echo 'testing the app 0..'
                
            }
        }

        stage("deploy") {
            steps {
                echo 'deploying the application ...'
            }
        }
    }
}
